import 'package:flutter/material.dart';

class AdminMainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Main Page"),
      ),
      body: Center(
        child: Text("Admin Main Page Content"),
      ),
    );
  }
}
